#include <stdio.h>
#include <conio.h>

int main()
{
   int R,A;
   printf("Take Radius of circle is: ");
   scanf("%d",&R);
   A=3.14*R*R;
   printf("Area of the circle is %d having the radius %d",A,R);
   getch();
}
